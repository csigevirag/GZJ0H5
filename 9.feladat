def prime_factor(number):
  factor=2
  while factor*factor<=number:
        while number%factor=0:
              number/=factor
        factor+=1
  if number>1:
        return number





print(prime_factor)
