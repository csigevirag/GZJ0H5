def longest(num):
  max=input('Adj meg egy mondatot')
  li=[]
  for i in max.split(' '):
      if i==i{::-1}:
         li.append(i)
  min=0
  for j in li:
      if len(j)min:
         j=min
   return j
  
print(longest)
